import { FinderDashboard } from '@/components/admin/finder/FinderDashboard';

export default function FinderPage() {
  return <FinderDashboard />;
}

export const metadata = {
  title: 'Business Finder - Admin Dashboard',
  description: 'Search for businesses across multiple data sources',
};